pasos:
git checkout main
git pull origin main
git checkout -b analisis-datos-tarea2 (para crear una nueva rama)

(Realiza tus cambios)
git add .
git commit -m "Desarrollado el modelo de predicción inicial"
git push -u origin seccion-modelado 

(Para fusionar trabajo)
git checkout main
git pull origin main
git merge rama nueva
git push origin main